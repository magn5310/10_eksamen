<header>
  <nav>
    <a class="logo" href="/"> <img src="/img/cehofski_silver.webp " alt="" /></a>

    <div class="nav-menu">
      <a href="/shop">
        <h4 class="navknap">shop</h4>
      </a>
      <a href="/shop#form">
        <h4 class="navknap">Contact</h4>
      </a>
      <a href="/about">
        <h4 class="navknap">about</h4>
      </a>
    </div>
    <div class="burger">
      <span class="bar"></span>
      <span class="bar"></span>
      <span class="bar"></span>
    </div>
  </nav>
</header>

<script src="../scripts/burger.js"></script>
<style>
  header {
    position: sticky;
    width: 100%;
    top: 0;
    height: 70px;
    background-color: var(--secondarycolor);
    z-index: 1000;
    transition: 0.3s ease;
  }
  .logo {
    margin-block: auto;
  }

  nav {
    background-color: var(--secondarycolor);
    display: flex;
    justify-content: space-between;
    transition: 0.3s ease;
  }
  a {
    position: relative;
    text-decoration: none;
    color: var(--primarycolor);
  }
  .navknap::after {
    content: "";
    position: absolute;
    background-color: var(--primarycolor);
    height: 1px;
    width: 0;
    left: 15%;
    bottom: 1rem;
    margin: auto;
    transition: 0.3s;
    transform-origin: center;
  }
  .navknap:hover::after {
    width: 75%;
  }
  div {
    display: flex;
    padding-top: 0.5rem;
  }
  h3,
  h4 {
    margin: 1rem 0.5rem 1rem 0.5rem;
  }

  .nav-menu {
    margin-right: 1rem;
  }

  img {
    width: 70%;
  }
  .navknap {
    font-size: 1.5rem;
  }
  .logo img {
    margin-left: 0.5rem;
    max-height: 50px;
  }

  .burger {
    display: none;
    cursor: pointer;
  }
  .bar {
    display: block;
    width: 25px;
    height: 3px;
    margin: 5px auto;
    background-color: var(--primarycolor);
  }

  @media (max-width: 800px) {
    .burger {
      display: block;
      margin-right: 1rem;
      align-self: center;
      padding-top: 1rem;
      transition: 0.3s ease;
    }
    nav.active {
      background-color: var(--primarycolor);
      transition: 0.3s ease-in-out;
    }
    .burger.active .bar:nth-child(2) {
      opacity: 0;
      transition: 0.3s ease;
    }
    .burger.active .bar:nth-child(1) {
      transform: translateY(8px) rotate(45deg);
      background-color: var(--secondarycolor);
      transition: 0.5s ease-in-out;
    }
    .burger.active .bar:nth-child(3) {
      transform: translateY(-8px) rotate(-45deg);
      background-color: var(--secondarycolor);
      transition: 0.3s ease-in-out;
    }
    .burger .bar:nth-child(2) {
      transition: 0.3s ease;
    }
    .burger .bar:nth-child(1) {
      transition: 0.5s ease-in-out;
    }
    .burger .bar:nth-child(3) {
      transition: 0.3s ease-in-out;
    }
    .nav-menu {
      position: absolute;
      z-index: -1;
      top: -1250%;
      gap: 0;
      flex-direction: column;
      background-color: var(--primarycolor);
      width: 100%;
      height: 100vh;
      transition: 0.3s ease;
      margin-right: 0rem;
      gap: rem;
      justify-content: center;
    }
    .nav-menu > * {
      text-align: center;
      transition: 0.3s ease;
    }
    .navknap {
      font-size: 6rem;
    }
    .nav-menu.active {
      top: 50%;
      transition: 0.3s ease;
    }
    .logo {
      padding-top: 1rem;
      align-self: center;
    }
    a {
      color: var(--secondarycolor);
    }
  }
</style>
